import {single_quotes_only} from './single-quotes-only.js'
import {use_return_value} from './use-return-value.js'

export const rules = {
    'single-quotes-only': single_quotes_only,
    'use-return-value': use_return_value,
}
